services:
  filehost:
    image: "replace_me"
    user: "${CONTAINER_UID}:${CONTAINER_GID}"
    x-millegrilles-certificat:
      roles:
        - filehost
      exchanges:
        - 1.public
      dns:
        localhost: true
        hostnames:
          - filehost
        domain: true
      split: true
    environment:
      - WEB_KEY=/run/secrets/key_cert.pem
      - WEB_PORT=1443
      - DIR_FILES=/var/opt/millegrilles/filehost/files
      - DIR_DATA=/var/opt/millegrilles/filehost/data
      - DIR_CONFIGURATION=/var/opt/millegrilles/filehost/configuration
      - CHECK_THROTTLE_MS=3
      - CHECK_BATCH_SIZE=5000000000
      - CHECK_BATCH_LEN=5000
      - LIST_INTERVAL=10800
      - LIST_STATS_INTERVAL=86400
    volumes:
      - ${MOUNT_FILEHOST}:/var/opt/millegrilles/filehost
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 500M
      restart_policy:
        condition: any
        delay: 20s
    configs:
      - source: mgca
        target: /run/secrets/millegrille.cert.pem
    secrets:
      - source: filehost
        target: key_cert.pem
      - source: passwd_redis
        target: passwd.redis.txt
    hostname: filehost
    networks:
      - app_net

networks:
  app_net:
    name: ${INSTANCE_NAME}_net
    external: true

configs:
  mgca:
    file: ${MILLEGRILLES_ROOT}/etc/millegrille.pem

secrets:
  filehost:
    file: ${MILLEGRILLES_ROOT}/secrets/filehost.pem
  passwd_redis:
    file: ${MILLEGRILLES_ROOT}/secrets/redis.txt
